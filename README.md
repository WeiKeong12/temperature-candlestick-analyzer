# temperature-candlestick-analyzer
A C++ console application that analyzes historical temperature data and visualizes it as text-based candlestick charts, with filtering and linear regression prediction.
